#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char newstr[] = "Programming is like building a multilingual puzzle\n";

	printf(newstr);
	return (0);
}
