#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 [Success]
 */

int main(void)
{
	int ascii_1_-_10 = 48;

	int ascii_a-f = 97;

	while (ascii_1_-_10 < 58)
	{
		putchar(ascii_a-f);
		ascii_1_-_10++;
	}

	while (ascii_a-f < 123)
	{
		putchar(ascii_a-f);
		ascii_a-f++;
	}
	putchar(10);
	return (0);
}
