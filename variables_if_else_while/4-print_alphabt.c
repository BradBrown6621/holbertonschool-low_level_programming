#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 [Success]
 */

int main(void)
{
	letters_l = 97;
	while (letters_l < 122)
	{
		if (letters_l = 101)
		{
			letters++;
		} else if (letters_l = 113)
		{
			letters++;
		} else
		{
			putchar(letters_l);
			letters++;
		}
		putchar(10);
		return (0);
}
