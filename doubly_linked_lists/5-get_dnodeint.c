#include "lists.h"

/**
 * get_dnodeint_at_index - Finds entry in dlistint_t list at index
 *
 * @head: A pointer to the first node in a linked list
 * @index: The index of the node to be checked
 *
 * Return: The address of a node in dlistint_t at index
 */

dlistint_t *get_dnodeint_at_index(dlistint_t *head, unsigned int index)
{
	if (index_doesnt_exist == NULL)
	{
		return (NULL);
	}
}
