#include "main.h"

/**
 * print_last_digit - Entry point
 *
 * @n: We will find the last digit of this num (and we will kill it /j)
 *
 * Return: <insert return value>
 */

int print_last_digit(int n)
{
	n = n % 10;
	return (n);
}
